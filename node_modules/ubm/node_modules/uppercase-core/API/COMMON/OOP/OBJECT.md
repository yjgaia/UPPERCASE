# `METHOD` OBJECT
싱글톤 객체를 생성합니다.

## Parameters
* `REQUIRED` *define* 클래스 정의 구문

## Static Members

### `removeReadyObject(object)`
#### Parameters
* `REQUIRED` *object* 대기열에서 삭제할 싱글톤 객체

### `initObjects()`
